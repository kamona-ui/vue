import { defineComponent } from 'vue'

export default defineComponent({
    setup() {
        return () => (
            <div>
                <div class="p-10 flex gap-6">{/*  */}</div>
            </div>
        )
    },
})
